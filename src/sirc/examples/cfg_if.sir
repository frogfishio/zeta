unit cfg_if target host

fn main(x:i32) -> i32 public
  block entry
    let c: bool = i32.cmp.eq(x, 0:i32)
    term.cbr cond:c, then:then_blk, else:else_blk
  end

  block then_blk
    term.ret value:111:i32
  end

  block else_blk
    term.ret value:222:i32
  end
end

