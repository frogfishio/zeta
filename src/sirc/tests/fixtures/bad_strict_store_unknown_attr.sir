unit bad_strict_store_unknown_attr target host

fn main() -> i32
  let p:ptr = ptr.sym(g0)
  ;; store.* only supports {align:int, vol:bool} scalar fields in --strict.
  store.i32(p, 0:i32) foo 1
  return 0:i32
end

