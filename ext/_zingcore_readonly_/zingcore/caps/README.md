# zingcore2.5 caps (WIP)

Capability implementations and selector modules for zABI 2.5.

Conventions:

- Cap identity is `kind/name` (e.g. `exec/run`).
- Selectors should be versioned (e.g. `run.v1`).
- Registration must be explicit; discovery must be enumerable.

Status: placeholder.
